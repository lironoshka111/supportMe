import styled from '@emotion/styled'

export const AlertWrapper = styled.div`
  position: absolute;
  width: 100%;
  display: flex;
  justify-content: center;
  top: 70px;
  p {
    font-size: 12px;
  }
`
